# AA VLA 项目交接（2026-09-11）

> **接手会话请先读完这份再动手。** 建议顺序：
> 1. 第一节「一句话现状」
> 2. 第二节「主表」—— 唯一能写进论文的数字
> 3. 第三节「已经钉死的结论」—— 不读会重复做已经做过的死路
> 4. 第六节「踩过的坑」—— 每条都是真实事故，且**每一条都是静默的**
>
> 预检：`source run/env.sh && python tools/preflight_stage3.py`
> 入口：`cd /root/autodl-tmp/VQAP && source run/env.sh`
> 方案登记：`python tools/exp_registry.py list`

---

## 一、一句话现状

**码注入臂第一次追平 baseline。** 把子任务指令换回整任务指令之后，
`B4L` 在 Seen18 上 38.44%，与 `B1@10000`（40.67%）和 `B0`（39.78%）
**无显著差异**（p=0.373 / 0.643）；而原方案 `B4` 是 32.44%，显著落后 8.22 pp（p=0.0021）。

**UnSeen 的泛化验证正在跑** —— 那才是码本价值主张的落点。

---

## 二、主表（全部 test split，逐局分数已归档）

### 2.1 Seen18 · 450 局 · 模板 planner · 噪声地板 1.72 pp

| 臂 | Seen18 | 说明 |
|---|---:|---|
| **B1@10000** | **40.67%** | baseline 最好点 |
| B0 | 39.78% | 零训练 |
| **B4L@10000** | **38.44%** | 整任务指令 + 码 ← **新方案** |
| B4X@10000 | 38.22% | B4L + 关细节码 + 原子门控 |
| B4@40000 flat | 36.22% | B4 权重但评测时换整任务指令 |
| B4@10000 | 32.44% | 子任务指令 + 码（原方案） |

**配对 McNemar**

```
B4 → B4L    Δ=+6.00   翻转117  45:72   p=0.0159  ✅ 显著改善
B1 → B4L    Δ=−2.22   翻转102  56:46   p=0.373   ❌ 追平
B0 → B4L    Δ=−1.33   翻转116  61:55   p=0.643   ❌ 追平
B1 → B4     Δ=−8.22   翻转139  88:51   p=0.0021  ✅ 原方案显著落后
B1@10k→@20k Δ=−6.44   翻转 97  63:34   p=0.0042  ✅ B1 过训
```

### 2.2 全量 28 任务 · 700 局 · 无 planner

| 臂 | Seen18 | UnSeen10 | 全部 28 |
|---|---:|---:|---:|
| B0 | 39.78% | 13.20% | 30.29% |
| B1@2500 | — | — | 30.71% |
| B1@5000 | — | — | 28.57% |
| **B1@10000** | **40.67%** | **15.60%** | **31.71%** |
| B1@15000 | — | — | 30.14% |
| B1@20000 | 34.22% | 15.60% | 27.57% |

### 2.3 B4 的 K 曲线 —— 🔴 两个 planner 下形状完全不同

```
              模板 planner(450局)      vlm-plan(280局)
K=10000           32.44%            Seen18 29.44 / UnSeen 5.00
K=20000           32.89%            Seen18 32.22 / UnSeen 11.00
K=30000           33.11%            Seen18 37.78 / UnSeen 11.00   ← vlm-plan 最好
K=40000           32.44%            Seen18 36.67 / UnSeen 10.00
极差               0.67 pp                 8.33 pp
```

**K 不能用模板法选。** 模板法下 B4 对 K 完全不敏感（极差 0.67 pp ≪ 噪声地板），
vlm-plan 下却有 8.33 pp 的跨度。最终报的是 vlm-plan 的数字，K 必须在 vlm-plan 下定。

> ⚠️ `B4@30000` 的 Seen18 是 37.78%，而此前一直用 `B4@20000 = 32.22%` 做判断 ——
> 「落后 8~12 pp」的印象里有一半是 K 选错造成的。

---

## 三、已经钉死的结论（**不要重复验证**）

### 3.1 🔴 子任务指令在**扣分**，不是「模型不读它」

旧机的 `flat` 消融结论是「无差别」（p=0.9/0.5）。**那个结论在 L4 档位下不成立。**
重做（test / L4 / 配对检验 / 逐局验证两份计划只有指令不同）：

```
B4@40000 模板法（子任务指令）  32.44%
B4@40000 flat（整任务指令）    36.22%    Δ=+3.78 pp，翻转 105 局 44:61
```

据此训出的 `B4L`（训练时就用整任务指令）实得 **+6.00 pp**，方向与量级都对上。

**连带结论**：`B2` 这个臂的意义要重估。它存在的理由是「扣除 planner 的功劳」，
但既然换语言本身就在扣分，`B4L vs B1` 这个对照（唯一差别是有没有码）更干净。

### 3.2 B1 在 10000 步达峰，20000 是过训

```
B0 → B1@10k    +0.89  p=0.796   无差别 ⇒ L4 没有损坏预训练表示
B1@10k → @20k  −6.44  p=0.0042  ✅ 过训
```

与旧机的 val 曲线（峰值 10000=43.7、20000=34.3）形状精确吻合。

### 3.3 B4 对训练步数不敏感（模板法口径）

```
B4@10000 与 B4@40000：均值逐位相同（都是 146/450），翻转 114 局 57:57，p=1.000
```

四倍训练量买不到任何东西。注入幅度同时在单调衰减（6.59% → 4.94%，
半衰期约 43,000 步，外推 60000 步跌破 3% 的健康判据）—— **多训只会让码更弱**。

### 3.4 🔴 码携带的是任务身份，不是动作语义

```
I(k_global; task)   = 1.93 bit      ← 码更像「任务指纹」
I(k_global; action) = 1.20 bit
I(k_global; action | task) = 1.10 / H(action)=2.97   扣掉任务身份只剩 37%
细节码 9 槽在样本内恒等：56,342 / 56,496 = 99.7%
```

复现：读 `data/atomaction_codebook_index.json` 的 `records`，按
`k_global` / `action` / `task` 算互信息，秒级。

### 3.5 长程任务是 headroom 所在

```
段数 vs B0 成功率  r = −0.410        段数 vs B1  r = −0.463
短程(≤3段) 3 个任务  B0 均值 64.0%
长程(≥5段) 13 个任务 B0 均值 32.9%      差 31.1 pp
```

PerAct 在一个关键帧只看到当前视觉 + 一句恒定指令；长程任务里**同一视觉状态
会出现在不同阶段**（stack_blocks 摞第 2 块和第 3 块画面几乎一样），
模型无从判断走到哪一步。这正是 planner + 码本本该补、而现在没补的信息。

### 3.6 评测噪声地板

```
450 局  1.72 pp      280 局  2.72 pp      180 局  ~2.72 pp      100 局  ~3.5 pp
```

判断改动是否有效必须做**配对 McNemar**（同 episode 索引 ⇒ 同物体摆放），
而不是比较均值。

### 3.7 Adapter 置信度**不能**判断码本覆盖

最佳阈值二分准确率 71.5%，随机基线 69.1%。Adapter 在没见过的场景上是
「自信地错」。这条路已排除。

---

## 四、机器与环境

```
路径      /root/autodl-tmp/VQAP        （旧机 /data0/xiexiao/VQAP 已不存在）
conda     /root/autodl-tmp/envs/aavla  py3.10 / torch 2.8.0+cu128 / numpy 1.26.4
GPU       4 × RTX 5090（32.6 GB, sm_120）—— 独占
          🔴 sm_120 必须 torch ≥ 2.7+cu128
CPU/RAM   100 核 / 754 GB      磁盘 750 GB，可用约 198 GB
git       用 SSH（deploy key 已装）。.git 曾积压 102 GB 垃圾，已清到 2.3 MB
```

### 🔴 Adapter 实时出码依赖三个外部模型，迁移时一个都没带

Seen 走模板法碰不到它们，**UnSeen 必须走这条路**，所以是在 UnSeen 评测时才集中暴露：

| 依赖 | 状态 |
|---|---|
| `openai` SDK | VLM 客户端走 DashScope 的 OpenAI 兼容端点。**旧机 pip_freeze 里也没有它**，是一直漏登记的依赖。已装 3.11.0 |
| DINOv2 `dinov2_vitb14` | Adapter 图像 tokenizer，`torch.hub` 首次下载 SSL 中断。现已缓存（346 MB） |
| CLIP `ViT-B/16` | 已下载并从 `.bin` 转出 `model.safetensors`，让 `_resolve_clip_path` 直连快照 |

### VLM 端点

```
主      DASHSCOPE / qwen3.8-max
备用    run/env.local.sh 里的 DMX（qwen3-vl-plus，1.7s/次）
        切换：AAVLA_VLM_BACKUP=1
🔴 换模型 = 换 planner，跨模型的成绩不可直接比较
```

---

## 五、当前状态与下一步

### 已就绪

- Seen18 replay 239,863 有效样本（8 个标签不可表示的样本已标为不可采样）
- 模板库按 Seen18 重建（233 条），停留先验覆盖 7 个任务
- **test 的 template / flat 计划已建**（各 450 条，Seen18 全覆盖）——
  `build_plans` 从 RLBench 直接读 `variation_number.pkl`，**不需要 test cache 也不需要 VLM**，
  所以 K 扫描、消融、A/B 对比**全部可以零成本做**
- 逐局分数归档 15 份（`scripts/archive_per_episode.py`）
- 方案登记 `tools/exp_registry.py`

### 进行中

```
B4L / B4X 的 UnSeen10 评测（各 250 局 vlm-plan，¥30/臂）
→ 「码本能不能带来泛化」的直接答案
```

### 还没做

| 项 | 说明 |
|---|---|
| B2 | 未训。但见 3.1 —— 它的必要性已被 `B4L vs B1` 削弱 |
| B4L 的 K 曲线 | 只有 10000 一点。vlm-plan 下 B4 在 30000 最好，B4L 未必同 |
| dense0 补 FiLM | FiLM 的 β 被 `SpatialSoftmax3D` 完全抵消，旋转分支收不到码 |
| 段内进度编码 | 针对 3.5 的 headroom，planner 已有停留先验 |
| X3 v4.0 保守接管 | 设计好了没实现。用模板法那条命中真值分段 92.5% 的确定性规则推进，只在 stall/quiet 时问 VLM |

---

## 六、🔴 踩过的坑（每一条都是静默的）

### 1. 训练/评测的注入层配置不一致 → 随机支路往 latents 注噪声

B4X 首次评出 22.22%，看似「改动让成绩崩了」。实为训练 `use_detail=False`、
评测却构造了 `use_detail=True` —— 多出一条**从未训练**的细节码支路，
`w_o ~ normal(std=0.02)` 直接注噪声。正确配置重评得 **38.22%**。

`load_weights` 只检查「ckpt 有、模型没有」的键；**反方向保持随机初始化且不打任何警告**。

**处置**：`use_detail` / `atom_gate` 上到 `Arm` 数据类，训练与评测都从臂读；
`load_weights` 新增硬失败（注入层参数不在 ckpt 里就抛）。
**教训**：能力必须写在臂上，不能放环境变量 —— `arms.py` 的既有注释早就写过这条。

### 2. `allowed_fields` 把码权限写在语言分支内部

`lang="task" + codes=True` 的臂会直接 return，**一个码字段都拿不到**，
训出来是没有码的 B1，且不报错、从 loss 和成绩都看不出来。
同一个 bug 的评测侧在 `eval.py`：planner 分支条件是 `uses_subtask_lang`，
B4L 会跳过整个分支、零码注入。两处均已修。

### 3. replay 里有标签不可表示的样本 → SE3 增广崩溃

`turn_tap` 有 8 个样本 `y=−0.657` 低于 scene_bounds 下界 0.157 m，
`trans_action_indicies=[22,−16,52]`。SE3 增广要求扰动后仍在界内，
本来就在界外的样本永远做不到，抽到必崩。

`point_to_voxel_index` 对上界 clamp、对下界不 clamp：低于下界 → 负索引 → one-hot 绕回；
**高于上界 → 被压成 99 → 连崩都不崩，更隐蔽**。

**处置**：`scripts/audit_replay_labels.py` 全量扫描，把缺陷样本 `terminal` 置 −1
（`is_valid_transition` 的既有机制，永久不被采样）。**任何 replay 重建后都该跑一次。**

### 4. 旧 ckpt 会让重训静默失效

`offline_train_runner` 用 `existing_weights[-1]`（最大步数）恢复。
换数据集重训前必须确认 `weights/` 里只有 `0`。

### 5. 在线 planner 没有候选措辞清单时打死分片

候选清单取自 train cache，UnSeen 天然没有。但 PLAN 的 prompt 无条件带三层措辞协议，
VLM 照办返回 `phrasing_id`，下游查不到就 raise —— **分片是交错切分的，
一个 UnSeen 任务会把同分片的 Seen 任务一起拖死**（9 个分片死亡，28 任务只剩 7 个）。

**处置**：没有清单时 prompt 换成「必须写 instruction」；代码侧拿不到措辞时降级兜底而非 raise。

### 6. eval_data.csv 的表头不会自己更新

YARR 的 CSVWriter 只在文件不存在时写表头。换任务集再评，列数变了表头不变，
新行与旧表头**逐列错位**；列数恰好相同时错位悄无声息。
已加 `rotate_eval_csv_if_schema_changed`，在 `cmd_run` 里只轮转一次。

### 7. 同一臂的两个评测并发会互撞 eval_data.csv

路径只按臂名分。**同臂并发必然互相覆盖**；不同臂并发是安全的。

### 8. 并发跑多个 stage3_eval 必须用 `--display-base` 错开号段

`pick_displays` 只扫「现在有没有 lock」，两个父进程同时扫会挑到同一批号。

### 9. `replay_loss_probe` 的 MASTER_PORT 写死

并行跑多个探针时除第一个外全死于 `EADDRINUSE`，而报错发生在 torch.distributed
初始化里，**表面上与 loss 探针毫无关系**。已改为自动挑空闲端口。

### 10. 评测分片的 spawn 子进程会成为孤儿

kill 父进程后，`multiprocessing.spawn` 的子进程仍占着显存。
清理时要按「不在训练进程的父子链里」精确筛，**绝不能无差别 kill**。

### 11. 迁移带过来的 `.git` 可能藏着上百 GB 垃圾

`.git` 曾是 102 GB：204 个中断 gc 残留的 `tmp_pack_*`（86 GB）+ 20,786 个不可达
松散对象（15 GB），而真正有效内容只有 2.2 MB。用 `git count-objects -vH` 查。

---

## 七、用户设定的工作规则

```
1. 设计文档仅供参考，不是约束
2. 分步规划，执行前必须先与用户确认
3. 有异议 / 发现缺陷 / 有更好建议要提出讨论
4. 每次都要报告改了哪些文件、加了哪些文件
5. 显卡：本机 4 张独占，用完即释放
6. 目录约定：权重 checkpoints/，结果 result/，日志 log/
7. 磁盘空间由用户负责，只需报告是否不够
8. 尽量避免过度测试（VLM 成本）；K 扫描与消融走模板法（零成本）
9. 汇总口径：报 mean 和 max，以 mean 为分析依据；两个臂必须同口径
10. 改进方案要能回退 —— 一个方案一个臂名，走 tools/exp_registry.py 登记
```

> ⚠️ 用户曾提出「用不同随机种子取 B4 的较高结果」。**这条没有执行** ——
> 只给某一臂挑最好的一次会让差值反映的是选择而非方法。正当替代：
> 多种子跑但**所有臂同等对待**，报 mean 和 max。

---

## 八、设计文档索引

| 文档 | 内容 |
|---|---|
| `Exp_Design.md` | 实验设计（Seen18/UnSeen8）+ 第七节：旧划分的历史结果，已封存 |
| `VLA_Design.md` | 任务划分 §2.4 / Stage 3 集成 / Planner / cache |
| `Adapter_Design.md` | Stage 2 Adapter 结构；§5 码向量注入的接口契约 |
| `VQAP_Design.md` | Stage 0/1 码本预训练 |
| `stage3/tasks.py` | **任务划分的唯一真源**（代码侧） |
| `stage3/arms.py` | **臂的唯一真源** —— 语言/码/注入层版本/use_detail/atom_gate |
| `tools/exp_registry.py` | 改进方案登记与回退 |
