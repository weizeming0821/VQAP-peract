# VQAP 实验设计

> **定位**：规定实验目的、路线、协议与判读口径。模型结构与训练细节见 [VQAP_Design.md](VQAP_Design.md)（Stage 0/1 码本）、[Adapter_Design.md](Adapter_Design.md)（Stage 2 Adapter + 注入契约）、[VLA_Design.md](VLA_Design.md)（Planner / Cache / Stage 3 集成）。
>
> **本文不重复**任务划分、四臂定义、训练配方等细节，一律引用 `VLA_Design.md`。

---

## 一、全局设定

### 1.1 任务与划分

**Seen 18**（官方 PerAct 18 任务全集，参与 Stage 3 微调）+ **UnSeen 8**（主干从未见过的任务，四臂全部零训练；Tier-A 6 个码本见过其原子片段 / Tier-B 2 个码本也没见过）。逐任务详情与五条硬约束的核验结果见 `VLA_Design §2.4`。

> **2026-09 改版**：原为 Seen12 / UnSeen6——在官方 18 任务**内部**按「是否参与 Stage 3 微调」切分。但主干在 600k 预训练中已见过全部 18 个任务，旧 UnSeen 撑不起「码本帮助模型泛化到未见任务」这一主张，故整体改版；`VLA_Design §7 T1` 的待办随之关闭。

### 1.2 对比臂

| 臂 | 语言条件 | 码注入 | 训练 |
|---|---|---|---|
| **B0** | 整任务指令 | 无 | 零训练（官方 `peract_600k`） |
| **B1** | 整任务指令 | 无 | 微调 K 步 |
| **B2** | 子任务指令 | 无 | 微调 K 步 |
| **B3** | 子任务指令 | FiLM + cross-attn | 微调 K 步 |

定义与冻结边界见 `VLA_Design §8.5`。

**B2 不可省**：B3 相对 B1 多拿了"任务被拆成子任务"这一额外信息。**只有 B3 > B2 才能证明码本本身有效**；B2 − B1 是 Planner 的功劳，不计入 VQAP 贡献。

### 1.3 评测协议

| 项 | 取值 |
|---|---|
| 脚本 | 基于 `source/peract/eval.py` 改造 |
| 局数 | **25 / 任务**（Seen18 用官方 test 全量）→ Seen18 = 450，UnSeen8 = 200 |
| `episode_length` | 25（每 episode 最多 25 个关键帧步） |
| 场景来源 | 从 test demo 重建，**同一 episode 对所有臂给出完全相同的初始场景** |
| 种子 | 单一种子（`start_seed: 0`） |
| 报告 | **逐任务分列 + 总均值 + 天花板归一化**；禁止只报总均值 |
| 统计 | **单种子，不报 mean±std** |

**公平性硬约束**（B1/B2/B3）：同 K 步、同数据、同 batch、同 LR schedule、同种子、同评测协议。论文报告各臂参数量。

> ⚠️ 改版前的三臂并未满足该约束（实测 B1 训到 40k、B3 训到 70k、B2 仅 step 0）。Seen18 重训是一次性把 K 对齐的机会，旧 checkpoint 全部作废。

**单种子的代价（如实记录）**：无法给出训练随机性的误差棒。判读以逐任务分列为主，**总均值的数个百分点差异不做强主张**。

**天花板归一化**：$\mathrm{sr}_{norm} = \mathrm{sr}_{policy} / \mathrm{sr}_{replay}$（逐任务）。天花板为 0 的任务从归一化均值中剔除并单独标注。

### 1.4 论文中必须声明的口径

> **【声明 A′｜UnSeen 的定义】** UnSeen 指 **PerAct 主干从未见过的任务**——既不在 `peract_600k` 的 **21 个**预训练任务中（已核其 `config.yaml`），也不在 Stage 3 的 Seen18 微调集中。四臂在 UnSeen 上**全部零训练**。
> 旧【声明 A】（「UnSeen = 未参与 Stage 3 微调」）随 2026-09 改版作废。

> **【声明 B′｜码本预训练范围与 UnSeen 的两层设计】** 码本与 Adapter 在 `AtomAction_Dataset`（69 任务 / 17 原子）上预训练。
> - **Seen18 侧**：官方 18 任务中 **13 个**与之有任务级重叠，**5 个不在**（`slide_block_to_color_target`、`sweep_to_dustpan_of_size`、`place_wine_at_rack_location`、`stack_cups`、`place_cups`）。详见 `VLA_Design §7 R2`。
> - **UnSeen 侧**：**Tier-A 6 个在**该 69 任务内——码本见过这些任务的原子片段，而主干没见过这些任务。**这正是被检验的假设**（原子知识能否跨任务迁移），不是数据污染。**Tier-B 2 个不在**，用于排除「码本恰好见过该任务」这一替代解释。两层分列报告，不合并求均值。

> **【声明 D｜物体级熟悉度】** UnSeen 8 个中有 3 个（`close_drawer` / `open_jar` / `take_money_out_safe`）与 Seen 任务**共享物体与场景、动作方向相反**，属「物体级熟悉、任务级全新」；其余 5 个为全新物体。逐任务分列时标注该属性，不在总均值里掩盖。

> **【声明 E｜UnSeen 的选取规则事先冻结】** 最终 8 个任务从**公开的 10 个候选池**中按**事先写定的规则**筛出：先剔除 E1 天花板为 0 的任务（该任务在关键帧动作模式下本就执行不了），再剔除 B0 零样本 sr ≥ 90% 的任务（无信号）。筛选**只用 B0 与天花板，在看到 B1/B2/B3 任何结果之前完成**。论文附完整候选池与筛选记录。

> **【声明 C｜原子成功由人工判定】** E7 的原子动作成功率由人工观看视频判定，**未采用随机打乱盲评**，判定者知晓实验条件。

---

## 二、实验路线

### 2.1 核心原则：一次评测，多重视图

> **一次评测运行 → 一份原始 rollout 记录；每个实验是对记录的聚合视图。**

B0 是固定权重、确定性评测，会被 E0/E2/E3 反复引用，但**只跑一次**。原始记录中同时保存成败、步数、子任务序列、码索引与 Planner 决策——即使当前不做码索引分析，记录它们是零成本的，日后补分析无需重跑评测。

**评测运行清单（4 臂 × 26 任务 × 25 局 + 天花板 26 × 25 = 3250 rollouts）**

| 运行 | 覆盖任务 | 被哪些实验读取 |
|---|---|---|
| `b0_official` | Seen18 + UnSeen8 | E0（管线校验，只看 Seen18）、E2、E3，**以及 UnSeen 定案筛选**（声明E） |
| `b1_seed0` | Seen18 + UnSeen8 | E2、E3 |
| `b2_seed0` | Seen18 + UnSeen8 | E2、E3 |
| `b3_seed0` | Seen18 + UnSeen8 | E2、E3 |
| `replay_ceiling` | Seen18 + UnSeen8 | E1（供 E2/E3 归一化，**以及 UnSeen 定案的第一道筛选**） |

> UnSeen 的 B0 与天花板两项**先行**：它们是声明E 的筛选依据，须在 B1/B2/B3 训练完成前跑完，且筛选不看 B2/B3 结果。

E7 另有独立的 rollout 流程（前缀重放 + 双指令条件）。

### 2.2 依赖与流程

```
┌── 前置（Seen18，✅ 均已完成）───────────────────────────────┐
│  官方数据生成（18 任务 × train100 / val25 / test25）✅         │
│         ▼                                                     │
│  Planner 离线分割 → cache（18 任务 train 全量 + val 抽检）✅    │
│         ▼  三道质量门禁（结构校验 / 统计体检 / 人工抽检 ≥95%）✅ │
│  码本 ✅ + Adapter ✅（均已预训练、冻结）                       │
└─────────┼─────────────────────────────────────────────────────┘
          ▼
   共享 Replay 全量重建（Seen18，239,871 样本 / 292.8 GB，三臂同一份）
          │                                    ⚠️ 环境迁移后须先过
          │                                       「环境等价性」关卡
     ┌────┴─────────────────────────┐
     ▼                              ▼
[训练] B1 / B2 / B3 各 K 步    [B0 官方权重，零训练]
     │                              │
     └──────────────┬───────────────┘
                    ▼
        [评测运行] 4 臂 × 26 任务 × 25 局 + 天花板 26 × 25
                    │
     ┌──────────┬───┴────────┬──────────────┐
     ▼          ▼            ▼              ▼
  E0 管线校验  E1 天花板   E2 Seen18    E3 UnSeen8 零样本
   (前提)     (含UnSeen)    (核心)     (核心, Tier-A/B 分列)

┌── UnSeen 支线（与 Seen18 训练并行）─────────────────────────┐
│  10 个候选 × test 25 局 数据生成（≈11.7 GB）                  │
│         ▼                                                    │
│  E1 天花板筛（剔天花板=0）→ B0 零样本摸底（剔 sr≥90%）        │
│         ▼  按【声明E】事先冻结的规则定案 8 个                  │
│  ⚠️ 不生成 train/val：UnSeen 不参与任何训练                    │
│     连带后果——模板 planner 无模板可用，B2/B3 改走 VLM planner │
└──────────────────────────────────────────────────────────────┘

[独立支线]
  E7 原子动作动机实验：前缀重放 → 双指令条件 → 录像 → 人工判定
```

**关键路径**：环境等价性关卡 → Seen18 replay 全量重建 → 三臂训练 → 评测。
UnSeen 的数据生成与定案走并行支线，只在评测阶段汇合。

**E0 必须最先判读**——它是所有后续结论的前提。

---

## 三、实验清单

| # | 实验 | 优先级 | 训练 | 评测 |
|---|---|---|---|---|
| **E0** | 评测管线校验 | **P0** | 0 | 复用 `b0_official` |
| **E1** | 天花板基线 | **P0** | 0 | `replay_ceiling` |
| **E2** | Seen18 四臂对比 | **P0** | 3 次 | 复用 |
| **E3** | **UnSeen8 零样本**四臂对比 | **P0** | 0 | 复用 |
| **E7** | 原子动作动机实验 | **P0** | 0 | 独立 |
| A1 | VQAP 模块消融（VASA / $L_{future}$） | P1 | 后续 | 后续 |
| A2 | 码本消融（全局码 / 细节码） | P1 | 后续 | 后续 |
| A3 | 真机实验 | P1 | 后续 | 后续 |
| E8 | Libero 跨域（码本迁移至 Libero 预训练 baseline） | **P2 · 暂不展开** | — | — |

> **A1 / A2 的成本提示**：引入 Stage 2 后，消融 VQAP 会触发**码本重训 → Adapter 重训 → Stage 3 重训**的三级连锁，每个消融臂都要走一遍完整流程。排期时按此估算。

---

## 四、P0 实验设计

### E0｜评测管线校验（前提）

**目的**：确认评测环境与协议正确，否则后续一切结论无效。

**做法**：判读 `b0_official` 的 450 条记录，逐任务对照 PerAct 已发表成绩（`VLA_Design §2.2`）。

**判读**：大致吻合即通过（官方 README 明言重新生成数据会有小差异）。若系统性偏低，**先查评测环境，不要改方法**。

### E1｜天花板基线

**目的**：区分「策略能力失败」与「该任务在此动作模式下本就执行不了」。

**做法**：把 test demo 的 ground-truth 关键帧位姿直接送进同一条动作管线执行，得逐任务 $\mathrm{sr}_{replay}$。

**输出**：逐任务天花板表（**26 个任务：Seen18 + UnSeen8 候选**），供 E2/E3 归一化。天花板为 0 的任务剔除并标注。

**UnSeen 侧的额外作用**：天花板是【声明E】筛选的**第一道**——天花板为 0 说明该任务在关键帧动作模式下本就执行不了，与策略能力无关，必须在定案前剔除。这道筛选**完全不涉及任何策略**，零污染。

### E2｜Seen18 四臂对比（核心）

**目的**：说明 VQAP 在训练过的任务上带来提升。

**做法**：读取四臂在 Seen18 上的记录（各 450 rollouts）。

**预期**：$\mathrm{sr}(B3) > \mathrm{sr}(B2) > \mathrm{sr}(B1)$，且 **B3 − B2 的缺口显著**。

**输出**：逐任务分列 + 总均值 + 天花板归一化 sr + 四臂参数量 + 学习曲线（堵"没训到收敛"的质疑）。

### E3｜UnSeen8 零样本四臂对比（核心）

**目的**：**证明码本能让策略在主干从未见过的任务上获得增益。** 这是全文最想主张的东西。

**做法**：**四臂在 UnSeen 上全部零训练**（B1/B2/B3 只在 Seen18 上微调过，从未见过这 8 个任务的任何一帧；B0 连 Stage 3 微调都没有），直接读各臂在 UnSeen8 上的记录（各 200 rollouts）。

**主张边界**：见【声明 A′/B′/D】。
- UnSeen 对**主干**是真正没见过的（不在 21 个预训练任务中，也不在 Stage 3 微调集中）；
- 对**码本**，Tier-A 6 个见过其原子片段——**这正是被检验的假设**，不是污染；Tier-B 2 个没见过，作对照；
- UnSeen 用到的原子全部在 Seen18 中出现过（`VLA_Design §2.4` 约束 ③），所以失败不能归因于「没学过这个动作」。

**判读口径**：以 **B3 − B2** 为核心——它是码本的净贡献，已扣除子任务分割的功劳。B3 − B1 为总增益，B2 − B1 归 Planner。

**预期**：
- **Tier-A**：$\mathrm{sr}(B3) > \mathrm{sr}(B2) \approx \mathrm{sr}(B1)$，且 B3 − B2 的缺口比 E2 更大（在没见过的任务上，码本携带的原子知识是唯一的额外信息来源）。
- **Tier-B**：增益应弱于 Tier-A。若 Tier-B 上 B3 仍显著领先，说明迁移的是原子级能力而非任务记忆，主张更强；若 Tier-B 无增益，则如实说明码本的作用范围限于其预训练覆盖过的任务族。

**输出**：Tier-A / Tier-B **分列**的逐任务表 + 各自均值 + 天花板归一化 sr + 与 E2 的 Seen ↔ UnSeen 增益对照表。**不合并 Tier-A 与 Tier-B 求总均值。**

### E7｜原子动作动机实验（论文核心动机）

**目的**：证明现有 VLA 学到的是**完整轨迹的拟合**，而非可组合的原子技能——**即便在它训练过、且成功率不低的任务上**，单独要求它执行其中一个原子动作时也做不好。这是"为什么需要原子动作码本"的实证支撑。

**步骤**：

1. **选题**：从 E0 结果中选 **6 个 $\mathrm{sr}$ 明显非零的任务**（$\mathrm{sr}\approx 0$ 的任务无对比意义），兼顾原子类型多样性。⚠️ 必须等 E0 实测后确定，**不得预先写死**。
2. **原子分割**：直接读该任务 test split 的 planner cache（segment 边界 + 动作标签 + 子任务指令）。
3. **原子 rollout（双条件）**：把场景重放到某 segment 的起始关键帧，再交给 policy：
   - **(a) 整任务指令条件**
   - **(b) 原子指令条件**

   每段步数预算 = 该 segment 的关键帧数 × 富余系数；每段录像。
4. **人工判定**：观看视频判定每次原子 rollout 是否成功。

**为何人工判定**：RLBench **只有整任务的 success detector，没有原子级判据**。自研 17 种判据既昂贵又易被质疑"判据设计偏向自己"。

**预期**：

$$\mathbb{E}[\mathrm{sr}_{atom}^{(b)}] < \mathrm{sr}_{complete} \qquad\text{且}\qquad \mathrm{sr}_{atom}^{(b)} \ll \mathrm{sr}_{atom}^{(a)}$$

- **第一式**是反直觉的核心现象：若原子彼此独立且模型真会做，整任务成功率应约等于各原子成功率之**积**，理应**低于**任一原子。实测若反过来，说明模型学的是整条轨迹的拟合。
- **第二式是更强、更干净的证据**（同一起始状态、只换指令）：若模型只是不认识"grasp the jar lid"这种措辞，那它拿到整任务指令时应当能正常做完这一段。$\mathrm{sr}_{atom}^{(b)} \ll \mathrm{sr}_{atom}^{(a)}$ 说明模型**对原子指令没有反应、只对整任务上下文有反应**——堵死"这只是指令 OOD"的质疑。

**规模**：6 任务 × 3 episode × ~5 原子 ≈ 90 段 × 2 条件 = **~180 段视频**，约 1–2 小时可评完。

**输出**：逐任务分列的三个数对比。

| 任务 | $\mathrm{sr}_{complete}$ | $\mathbb{E}[\mathrm{sr}_{atom}^{(a)}]$（整指令） | $\mathbb{E}[\mathrm{sr}_{atom}^{(b)}]$（原子指令） | 各原子 sr 明细 |
|---|---|---|---|---|

**⏸ 待定**：① 用 B0 还是 B1 作为被测模型；② 前缀重放交接的 `eval.py` 改造方案（与 E1 共用）。

---

## 五、视频录制规范

**所有评测脚本（E1/E2/E3/E7 及后续消融）都必须保留视频录制能力，且策略、命名、路径一致。**

### 现有机制的限制

`helpers/custom_rlbench_env.py` 在 `reset()` 时就按 `episode_index % record_every_n` 决定是否录制，**此时还不知道该 episode 会不会成功**，因此无法直接实现"只保留成功的"。

### 改造方案

把「是否录制」与「是否落盘」解耦：

```
reset()        → 恒开录制，清空帧缓冲
episode 执行中  → 按帧间隔累积到内存缓冲（帧间隔可配，控制内存与体积）
episode 结束    → 按保留策略决定是否写盘：
                   keep_policy = success_only（默认）| all | fail_only
```

既满足"挑成功的留档"，又避免把失败 rollout 的视频全部写盘造成体积膨胀。

**两级画质**

| 级别 | 用途 | 配置 |
|---|---|---|
| L1 · 常规留档 | 所有实验默认 | 320×180，成败写入文件名 |
| L2 · 展示精选 | 论文 / 汇报 | 1280×720（`cinematic_recorder`），对选定的 `(task, episode)` 定向重跑 |

L2 需要 `eval.py` 支持「只跑指定 `(task, episode)` 列表」，是一处小改造。

> ⚠️ **需实测**：帧在 episode 内累积于内存。一个 episode 含 25 个关键帧步，每步有大量运动规划子步，帧数可能上千（320×180×3 ≈ 172 KB/帧）。**全量录制前须实测单 episode 的帧数与内存峰值**，据此设定帧间隔，必要时改为边录边落盘。

---

## 六、已知风险

| # | 风险 | 处置 |
|---|---|---|
| 1 | **UnSeen 地板风险** —— 新 UnSeen 是主干真正没见过的任务，四臂可能全部趴在地板（旧的「信号不足」问题已由 2026-09 改版解决，见 `VLA_Design §7 T1`） | 三重缓解：① 8 个中有 3 个「同物体反向」任务作保险；② 定案前先跑 E1 天花板 + B0 摸底剔除无信号任务（【声明E】）；③ 10 个候选全部生成数据，无论筛选结果都不必补生成。**结果中必须同时报 B0 作参照** |
| 2 | **B2 可能拿到子任务指令却用不上** —— 能适应语言分布变化的只有 `lang_preprocess` 单层线性（0.066 M），其后全部冻结 | 详见 `VLA_Design §8.8 P1`。**若 B2 ≈ B1，不得直接下结论说子任务分割无用**，更可能是容量瓶颈；备选方案已留档 |
| 3 | **码本语义错位** —— Adapter 学的是 AtomAction phase 的边界，用的却是 PerAct 关键帧归组出的 segment | 详见 `VLA_Design §7 R1`；诊断方案见 `§7 D1`，建议在正式建 cache 前先做 |
| 4 | **索引-语义无监督** —— Stage 3 不存在任何"码索引 ↔ 原子语义"的显式监督，损失只有 PerAct 原生四个 CE | 不影响 sr 主结论；论文中不宜对"码本语义可解释性"作强主张 |
| 5 | **细节码 9 槽恒等** —— 样本内唯一索引数 1.00 / 9（`Adapter_Design §7 R1`） | A2 中细节码消融的结论会失去意义；报告细节码指标时必须注明只有 1 个自由度 |
| 6 | **单种子无误差棒** | 每任务 25 局提供任务级样本量；不报 mean±std；总均值的小幅差异不做强主张 |
| 7 | **在线 Planner 分布漂移** —— 在线输出若偏离离线 cache 分布，B2/B3 会系统性劣化 | `VLA_Design §6.1` 的硬约束：离线与在线共用同一段指令格式规则文本与同一套后处理路径 |
| 8 | **5 个部署任务不在码本训练集中**，其中两个恰是"独占原子任务"（`place_cups`→`hang`、`place_wine_at_rack_location`→`insert`），而这两个原子在 AtomAction 中样本最稀缺（100 / 283 条） | 见【声明 B′】与 `VLA_Design §7 R2`。如实记录，不为此调整训练数据 |
| 8b | **审稿人可能质疑 Tier-A 是数据泄漏** —— 码本见过 UnSeen Tier-A 任务的原子片段，而 B1/B2 没有任何该任务的信息 | 这正是码本作为**可复用预训练模块**的价值主张（类比预训练视觉编码器），必须由【声明 B′】明写、不得含糊。Tier-B 2 个对照任务专为堵这条质疑而设 |
| 9 | **存储需求**（共享 replay） | 已实测定量：Seen12 = 165,773 样本 / 203 GB；**Seen18 = 239,871 样本 / 292.8 GB**（`replay_capacity` 300,000 仍有 20% 余量）。磁盘已扩容至 750 GB；全量重建需先删旧工件（两份不可并存） |
| 12 | **环境迁移导致的数据混源**（2026-09） —— 整仓从旧机迁到新机，torch 2.4.1+cu121 → 2.8.0+cu128、GPU 换到 sm_120 | **已实测结论：采用 Seen18 全量重建**（18 任务由同一环境同一次运行产出，从根上消除混源），并在删除旧工件**之前**跑完了「环境等价性」关卡（G1，见下）|
| 12a | **G1 环境等价性实测结果**（`open_drawer` 全量 3,785 样本逐字段比对，报告存 `result/p5_train/g1_open_drawer_verify.txt`） | **34 个几何/结构字段 + 4 个字符串字段全部逐位相同**（含 RGB、点云、相机内外参、`trans_action_indicies`、`rot_grip_action_indicies` 与全部码字段）。`reward` 仅在 1,125 个 `terminal=-1` 的 `add_final` 填充帧上不同（两边都是未初始化内存，`is_valid_transition` 永久排除，训练中永不被采样，已逐帧核实差异**只**出现在这些帧上）|
| 12b | **语言嵌入的 fp16 末位差异**（4 个 `*_emb` / `*_token_embs` 字段） | **原定的 `cos ≥ 1−1e−6`、`max\|Δ\| ≤ 1e−5` 在物理上不可达**：CLIP RN50 **以 fp16 前向**，量级 9 处 fp16 的 1 ULP 就是 7.8e−3。实测差异**按指令确定性**（同一句指令的全部样本 ULP 倍数完全相同）⇒ 纯算术差异而非损坏；新环境自身连跑两次逐位相同。全体范围 `1−cos ∈ [1.9e−6, 1.56e−5]`、相对误差 `∈ [2.7e−4, 4.9e−3]`。**修订判据：`1−cos ≤ 3e−5` 且相对误差 ≤ 1e−2**（约 2 倍余量），并在新 manifest 记录迁移备注 |
| 12c | **该差异对模型的实际影响（实测，非阈值论证）** | 同一份 B1@40k 权重、同一批内容配对样本，唯一变量是嵌入来自新旧硬件：total_loss 1.176208 → 1.176080，**相对差 1.1e−4，是单批标准差的万分之 1.7**。⇒ 对训练无实质影响。且全量重建后 18 任务内部完全自洽，旧↔新差异只影响「与旧 checkpoint 对照」这一件事 |
| 13 | **游离文件破坏 RLBench 的数据集断言**（2026-09 实测遇到） —— `train/open_drawer/.../episode0/front_rgb/` 下混入 Jupyter 的 `.ipynb_checkpoints` 目录，使该目录条目数 131 ≠ 其余相机的 130，`get_stored_demos` 抛 `RuntimeError: Broken dataset assumption`。旧 replay 建成之后才产生（9 月 6 日），全量重建时会在该 episode 上硬失败 | 已删除（副本与原图 MD5 完全相同，原图无损）；全库复扫无其它隐藏残留。**今后任何浏览过数据目录的操作之后、正式重建之前，都应先跑一次隐藏文件扫描** —— 这类缺陷不会被 `audit_report.json` 的 episode 计数发现（它只数 episode，不数每个相机目录的文件） |
| 10 | **评测管线未经验证** | E0 作为前提门禁，未通过不得进入 E2/E3 判读 |

---

## 七、历史结果（旧 Seen12 / UnSeen6 划分，2026-09-09 封存）

> 🔴 **本节是旧划分下的历史记录，不与新主表混读。** 它产出于 2026-09 改版之前：
> 任务集是 Seen12、replay 是 `seen12` 工件、三臂**未满足等 K 约束**
> （B1@40k / B3@70k，B2 在本机只留 step 0），且逐局归档 `result/p7/versions/`
> **没有随迁移带过来**，因此这些数字无法再做配对复核。
>
> 保留它的理由有三条，都与新实验直接相关：
> ① 评测噪声地板（同权重重跑 4 次标定）在新机上仍是判读任何差值的前提；
> ② `flat` 对照与码本门控两个消融的结论是**机制性**的，不随划分改变；
> ③ 它记录了核心主张第一次被证伪的完整过程，新主表必须解释或推翻它。
>
> **新划分（Seen18 / UnSeen8）下的主表尚未产出**，产出后单列一节，本节保持原样封存。

> **口径**：Seen12 · 每任务 25 局 = 300 局。
> 评测非确定性（RRT 运动规划随机），已用**同权重重跑 4 次**标定噪声：
>
> ```
> 整体 300 局：单次 SD 1.49 pp，两次之差 SD 2.11 pp
> 单任务 25 局：中位 |Δ| 4.0 pp，|Δ|≥8pp 占 44%，≥12pp 占 12%，≥16pp 占 2%
> ```
>
> **逐任务 ±12 pp 以内的涨跌不可解读。** 判断改动是否有效必须用**配对 McNemar**
> （同 episode 索引 ⇒ 同物体摆放），而不是比较均值。

---

### 7.1 主表：test split（held-out，每格完整 300 局）

| 任务 | B0<br>零训练 | B1<br>整任务+微调 | B2<br>子任务指令 | B4<br>v2码注入 | B4·X3 v3.6<br>在线planner |
|---|---:|---:|---:|---:|---:|
| close_jar | 44 | 44 | 28 | 36 | 32 |
| light_bulb_in | 4 | 16 | 12 | 16 | 28 |
| open_drawer | 92 | 76 | 84 | 88 | 72 |
| place_cups | 0 | 0 | 0 | 0 | 0 |
| place_shape_in_shape_sorter | 0 | 8 | 12 | 8 | 20 |
| place_wine_at_rack_location | 44 | 48 | 20 | 16 | 20 |
| push_buttons | 28 | 32 | 24 | 12 | 32 |
| put_groceries_in_cupboard | 20 | 16 | 12 | 0 | 8 |
| reach_and_drag | 92 | 60 | 88 | 88 | 56 |
| slide_block_to_color_target | 64 | 56 | 56 | 48 | 24 |
| stack_blocks | 4 | 40 | 16 | 24 | 12 |
| sweep_to_dustpan_of_size | 64 | 76 | 56 | 52 | 48 |
| **均值** | **38.00** | **39.33** | **34.00** | **32.33** | **29.33** |

#### 配对检验

| 对比 | 归因 | Δ | 翻转局 | p | 判定 |
|---|---|---:|---:|---:|---|
| B0 → B1 | 微调本身的效果 | +1.33 | 84 | 0.744 | ❌ 无差别 |
| B2 → B1 | 子任务指令的代价 | +5.33 | 112 | 0.156 | ❌ 不显著 |
| **B2 → B4** | **码本的功劳（核心主张）** | **−1.67** | 63 | 0.615 | ❌ **不成立** |
| **B1 → B4** | 完整方案 vs baseline | **−7.00** | 99 | **0.044** | ✅ **显著变差** |

> 🔴 **核心主张 `B4 > B2` 在 held-out 测试集上不成立**，且 B4 显著差于 B1。
> val 上曾观察到的 `B4 − B2 = +4.67 pp`（p=0.14）**没有复现**。

---

### 7.2 val split（选点与消融用）

| 臂 | mean | max | 局数 |
|---|---:|---:|---:|
| B1 整任务+微调 | 35.83 | 36.33 | 600（2 次） |
| B0 零训练 | 35.67 | — | 300 |
| B3 v1 码注入 | 34.33 | — | 300 |
| B4 v2 码注入 | 33.67 | 35.00 | 600（2 次） |
| B2 子任务指令 | 30.33 | — | 300 |

**B4 的 val 曲线（合并两次测量，600 局/点）**

```
ckpt      2500   5000  10000  20000  30000  40000  60000
均值      17.00  19.67  26.67  32.67  32.00  33.67  31.83
```

真实上升只发生在 2500 → 20000（+15.7 pp）。**20000 之后是平线** ——
40000 比 20000 高 1.00 pp = 0.7σ，60000 比 40000 低 1.84 pp = 1.4σ，都分辨不开。
**K = 40000 是点估计最大值，但"40k 是最优点"这个说法数据不支持。**

---

### 7.3 三个消融，全部指向同一结论

#### ① `flat` 对照：子任务指令的语言内容对 PerAct 无影响

把子任务指令**全部换成整任务指令**，码与分段完全不变：

```
B4 模板法（子任务指令）  33.67%  [min 32.33 / max 35.00]
B4 flat（整任务指令）    34.33%
                        Δ = −0.67 / +2.00 pp   p = 0.906 / 0.545   ❌ 无差别
```

**PerAct 根本没在用子任务指令里的信息。** 这封死了整条在线 planner 路线 ——
指令措辞、模板库、变体先验优化的都是一个不影响输出的输入。

#### ② 码本覆盖门控：能涨点，但只到与 B1 持平

18 个任务里 5 个不在 `AtomAction_Dataset`（Seen12 val 中占 4 个）。
对它们把 `subtask_code_mask` 置 0：

```
                  全部12    覆盖8    未覆盖4
B1 基线            35.33    33.50    39.00
B4 原版            33.67    36.50    28.00
B4 门控            36.00    35.50    37.00      ← 未覆盖组 +9.00 pp
```

> ⚠️ 门控**不改动**覆盖 8 任务，那三次的覆盖组分数（39.50/33.50/33.50）
> 是同一个量的三次独立测量，极差 6.0 pp —— 不可读成"门控让覆盖组下跌"。

#### ③ Adapter 置信度**不能**判断码本覆盖

1673 次带置信度的码调用：

```
              n     中位     p25    p50    p75
覆盖 8      1156   0.907    0.581  0.908  0.943
未覆盖 4     517   0.678    0.332  0.678  0.924

最佳阈值 τ=0.20 的二分准确率 71.5%，随机基线 69.1%  →  仅比瞎猜好 2.4 pp
```

`sweep`（未覆盖）0.907、`slide_block`（未覆盖）0.823 都很高；
`light_bulb`（覆盖）0.604、`put_groceries`（覆盖）0.703 反而低。
**Adapter 在没见过的场景上是"自信地错"。**

---

### 7.4 注入层状态：健康，但可能在加噪

```
tools/b4_health.py --arm B4 --step 40000
  注入幅度 4.78%（判据 ≥3% OK）
  参照：v2 初始 16.79%（未训练）· v1 训练 100k 步后 0.0095%（被 LAMB 锁死）
  B4 训练 loss 2.90  vs  无码基线 B2 loss 2.46
```

v2 注入层解决了 v1 的退化问题，**码确实到达了模型**。但**带码的训练 loss 更高**，
配合 7.3② 的门控实验，一致解释是：**注入的码对相当一部分样本是噪声**。

**v1 失效的根因（已量化）**

```
① 全局码与语言高度冗余     H(k_global)=3.941 bit
                          H(k_global | 子任务指令)=0.910 bit   → 指令已解释 77%
② 细节码 82.8% 退化        9 个槽位完全相同的段 5652/6824
③ 门控被 LAMB 锁死         ‖Δp‖ ≡ lr·‖p‖，零初始化张量步长恒为微量
④ FiLM 的 β 被抵消         SpatialSoftmax3D 对空间常数偏置不变（softmax(x+c)=softmax(x)）
```

---

### 7.5 评测方案对比

| 方案 | 说明 | B3@40000 val | B4@40000 |
|---|---|---:|---:|
| **X1 `template + plan`** | 离线模板计划 + 查表码 | **35.33%** | 33.67%（val） |
| X2 `template + adapter` | 离线计划 + 实时 Adapter 出码 | 34.33% | — |
| X3 `vlm-plan + adapter` | 在线 VLM 规划 + 实时出码 | 见 7.6 | 29.33%（test, v3.6） |
| `flat` | 子任务指令 → 整任务指令 | — | 34.33%（val） |

### 7.6 在线 planner（X3）版本史

```
B3@40000 · val · 120 局
  v3.0  26.67    v3.1  29.17    v3.2a  26.67    v3.2c  33.33    v3.3  34.17

B4@40000 · val · 300 局 · 同 11 任务口径（X1 同口径 32.36）
  v3.3  27.64    v3.4  27.64    v3.5  29.45

B4@40000 · test · 300 局
  v3.6  29.33
```

> 🔴 v3.3 在 B3、v3.4/3.5 在 B4、v3.6 在 test —— **三段不能连读**。

| 版本 | 改动 | 机制验证 | 成绩变化 |
|---|---|---|---|
| v3.2c | 关掉执行记忆 | REPLAN 173 → 6 | +6.66（唯一有机制支撑） |
| v3.3 | 夹爪语义分类 | 压制 186 次误触发 ✅ | 无差别 |
| v3.4 | RETRY 位置回退 | 触发 157/300 局 ✅ | **Δ=+0.00, p=1.000** |
| v3.5 | 末段禁用 NEXT + EXTEND | 末段空转 500 → **0** ✅ | +1.82（噪声内） |
| v3.6 | 先验展开（transfer×2） | 段数 4 → 5 ✅ | 未修好目标任务 |

**六个版本、四轮机制改动，每次机制都精确生效，成绩都没动** ——
与 7.3① 的 `flat` 结论完全一致：优化的是一个不影响输出的输入。

---

### 7.7 与目标的差距

用户目标是 **X3 + B4 超越 B1 十个百分点**。test 上的现实：

```
B1           39.33          目标 ≈ 49.3
B2           34.00          拆子任务本身先扣 5.33 pp
B4           32.33          码本没有补回来，反而再降 1.67 pp
B4 · X3      29.33          在线 planner 再降 3.00 pp
```

**当前所有改动都在填"拆子任务"造成的坑，没有一项产生净正收益。**
